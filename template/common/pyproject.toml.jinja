[project]
name = "{{ project_slug }}"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">={{ python_version }}"
dependencies = [
{% if package_mode == "package" %}
  "typer>=0.16.0",
{% endif %}
]

[dependency-groups]
dev = [
  "mypy>=1.11.0",
  "pre-commit>=4.0.0",
  "pytest>=8.3.0",
  "ruff>=0.6.0",
]

{% if package_mode == "package" %}
[project.scripts]
{{ project_slug }} = "{{ project_module }}.__main__:main"
{% endif %}

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

{% if package_mode == "package" %}
[tool.hatch.build.targets.wheel]
packages = ["src/{{ project_module }}"]
{% endif %}

[tool.ruff]
line-length = 100
target-version = "py{{ python_version | replace('.', '') }}"
src = ["src", "."]

[tool.ruff.lint]
select = [
  "E",
  "F",
  "I",
  "B",
  "UP",
  "SIM",
]
ignore = []

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.mypy]
python_version = "{{ python_version }}"
strict = false
warn_unused_configs = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_return_any = true
no_implicit_optional = true
check_untyped_defs = true
pretty = true
{% if package_mode == "package" %}
mypy_path = ["src"]
{% endif %}

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.uv]
package = {{ "true" if package_mode == "package" else "false" }}
